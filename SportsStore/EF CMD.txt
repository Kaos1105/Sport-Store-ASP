//prepare the database for its first use--dotnet ef migrations add Initial//create a database migration that will allow Order objects to be stored in the database--dotnet ef migrations add Orders//delete database--dotnet ef database drop --force//recreate the database and apply the migrations --dotnet ef database update//add Shipped Orders--dotnet ef migrations add ShippedOrders// create a new migration for the Identity database--dotnet ef migrations add Initial --context AppIdentityDbContext// create the database and run the migration commands--dotnet ef database update --context AppIdentityDbContext//migrate the database to the new schemadotnet ef database update